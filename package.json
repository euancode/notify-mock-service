{
  "name": "notify-mock-service",
  "version": "1.0.0",
  "description": "A mock implementation of the GOV.UK Notify REST API with a live dashboard.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "deploy:render": "node scripts/deploy-render.js",
    "tunnel": "node scripts/tunnel.js"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18"
  }
}
